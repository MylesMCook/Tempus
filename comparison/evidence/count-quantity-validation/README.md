# Count and quantity task coverage

The authored Tempus journey report now covers 25 tasks, up from 22. The new tasks confirm a shopping quantity through date/DST correction and exercise both policies for an excluded occurrence in a counted weekly schedule. Each checks original text, source span, title, complete output and calendar-file expansion. The shopping task needs three answers; this work does not show whether unfamiliar users find that burden helpful.

The new edit cases change three apples to four, three occurrences to four, or the excluded date from September 21 to September 28. Each result equals a fresh parse without the old selection; both file-preparation entries reject the unresolved edited input. The earlier 22 tasks still use their existing whitespace-edit checks. Broader meaningful-edit coverage remains open.

The [current source snapshot](../count-quantity-meaningful-edits/README.md) retains 31 comparison inputs, 25 authored journeys and all 25 files. Existing comparison expectations and the nine product requirements are unchanged. Only the journey corpus advances to correction-journeys-development-v2-count-quantity. The preceding [25-task snapshot](../count-quantity-journeys/README.md) retains the initial whitespace-only version; the prior 22-task reports remain preserved. Do not compare different journey denominators as an accuracy improvement.

Current documentation archive 4b96da77 matches every source result and file hash in the 31/25 replay. The updated replay runner accepts explicit edited input while retaining the old whitespace fallback; a separate 31/22 legacy replay passes. Installed SDK implementation is unchanged. Three focused comparison test files pass (13 tests), including the single harness that executes all 25 tasks. Type checking and scoped lint pass. The first type check failed because the preserved historical test copy still had a .ts extension and was compiled from the wrong directory. Renaming that byte-preserved history artifact to .ts.txt removes it from compilation; the rerun passes.

The separate Python readers match all 25 bounded files and the existing Tokyo ongoing fixture within their declared windows. They verify exact bytes, dates and titles against authored expectations. They are independent reader implementations, not independent expected-answer authors, participants or calendar clients. The existing ongoing-duration rule remains nonconforming: the explicit --require-rule-conformance run exits 1. Its failure and raw reader report are retained here.

There is still no gpu-time correction interaction adapter or comparative count-policy task evaluation. The 31-case direct comparison remains unchanged; these additions must not be called new competitive wins. No holdout was opened, no app UI changed, and no browser/device/import result is claimed. Nothing was pushed, published or written to calendars.
