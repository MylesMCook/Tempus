# Packed Worker evidence

Local workerd executed the installed SDK archive `4ec180308a16144daa73f3c154d22868f8f47fcd9c4bcebf81d2be2ec1b452df` through Wrangler 4.131.1. Strict installed TypeScript declarations passed. The preceding package runner compared all 56 installed files with that archive.

The fixed diagnostic checks all seven export failure codes, metadata repair, stale-answer rejection, a future repeated-clock correction and same-month shared-time correction. Its complete files contain nine weekly intervals, two timed points and four monthly intervals. Python icalendar 7.3.0 / recurring-ical-events 3.8.2 read back the expected dates, endpoints and titles; points retain no invented duration.

`worker-result.json` retains the exact generated file strings. `reader-report.json` records their hashes and expanded occurrences. `provenance.json` identifies the source and routing checks. JSON presentation was formatted before checksums were frozen.

[Reproduce against a packed installation](../../../examples/sdk/README.md#reproduce-the-packed-worker-check). These are authored integration checks, not independent accuracy evaluation. Local workerd is not a deployed Worker. File reading is not calendar-client import. No calendars were written and the task-local server was stopped afterward.

Run `shasum -a 256 -c SHA256SUMS` from this directory to check retained bytes.
