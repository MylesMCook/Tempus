# Count-policy SDK verification

Local archive `7fa6dd5769dc486a4de6e216984d99b57d1f7c97c64b193f94c6766e582c9d5e` contains the current count-exclusion, excluded-clock and written past-start source. All 56 installed files match the archive. Six integration examples pass on Node 22.12.0 and 26.8.1, including five completed past-start/exclusion cases. Installed consumer TypeScript and both browser entry-point import closures pass. Installation was offline with lifecycle scripts disabled. No package was published.

Six desktop browser runs pass with 1.1-second download spacing: Chrome 153, Playwright Firefox 148 and WebKit 26.4 at widths 320 and 1280. WebKit uses explicit Option-Tab. Every run checks original input, offered correction, complete export and invalidation after editing, with eleven actual downloads: date-only point, all-day range, explicit timed range, weekly interval, finite list, weekly/monthly counts and both past-start/exclusion count policies. No page errors or external requests were recorded. The installed package and copied consumer hashes are verified before execution.

The first unpaced run failed in Chrome at both widths while waiting for the eleventh download, after ten successful files and no page errors. Firefox and WebKit passed. Its report and 64 downloaded files remain under `browser-failed/`; the strict reader correctly rejected that incomplete run. Only download spacing changed for the retry. The paced run passes, but the precise cause of the rapid-download failure is unconfirmed. These are correctness checks, not throughput measurements or proof that rapid repeated downloads work.

The same installed archive passes the local workerd handler through Wrangler 4.131.1, with twelve file outputs, seven failure codes and correction/stale-answer checks. Copied Worker source matches repository source and passes installed TypeScript. This is local execution, not a deployed Cloudflare check.

Locked Python icalendar 7.3.0 and recurring-ical-events 3.8.2 read all **66 paced browser downloads and twelve Worker files**. Fixed expectations check dates, titles and precision; counted outputs contain exact future starts with no ongoing rule or invented duration, including ten-year expansion to detect extra events. These readers are separate implementations; cases and expected answers remain authored development fixtures, not independent evaluation. No calendar-client import was attempted.

Reproduce using `examples/sdk/README.md`: create a new verified packed installation, invoke `verify-installed-node.mjs` with the second Node executable, run `verify-import-closure.mjs`, then serve the copied browser and Worker consumers on loopback. Run `verify-browser.mjs` with `--webkit-option-tab` and both locked Python readers. Existing evidence directories are refused. Reports preserve their identities with only home paths redacted; `provenance.json` records original hashes. Screenshots remain in local scratch. `source-hashes.json` identifies the runners and readers; task-owned lint passes.

The main-app milestone remains [804 shared tests and twelve source journeys](../count-past-journeys/README.md). No application behavior changed during this packaging work. Comparison replay still predates counts, and performance still belongs to archive `47a218ba`. Actual imports, physical devices, Safari.app, retail Firefox, offline browser startup, deployed Workers, independent evaluation and accumulated post-scan review remain open. Passing these checks does not establish competitive superiority.

Both verification servers exited after interruption (status 130); ports 5184 and 8788 had no listeners. The main app remained on loopback port 5174. Their shutdown invoked the existing repository prepare hook. No push, publication, deployment, cloud mutation or calendar write occurred.
