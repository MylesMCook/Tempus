# Explicit ranges: packed Node and replay

Archive `47a218ba893908d49fdd132122040df95d107df8508be22fd6ff25a7fad342f4`, September 13, 2026. All 56 installed files match after offline installation with scripts disabled. Five installed examples pass on Node 22.12.0 and 26.8.1, including explicit numeric-date/DST range correction, file preparation, stale edits and conflicting-answer rejection. Installed TypeScript checks and static browser import closure pass with the same three declared packages.

Review found that contradictory interval-clock answers were accepted by taking the first answer. A regression test failed with `resolved` instead of `needs-clarification`. The fix requires a single distinct clock answer per endpoint; conflicts reopen clarification. A separate four-step test retains both endpoint dates and clocks and verifies dependent-answer invalidation. The complete run passes 765 shared-source plus 16 harness test cases. The replay counts as one test, not 22 independent tests.

The [new development snapshot](../explicit-range-journeys/README.md) has 31 inspected comparison cases and 22 complete scripted tasks. Both readers pass all 22 files. The separate strict duration-rule command still exits 1 on its known conformance mismatch. Existing comparison summary and valueSummary are unchanged. The before-fix failure summary is retained in review.json.

The first post-fix main-app run exited on an unhandled download timeout before saving its report; it is not counted as a pass. The runner now handles download waiting and activation together so either rejection is caught. Its retry passes all four main-app journeys and independent readback; files and the failure summary are retained. The initial timeout cause was not isolated. Browser and local Worker execution against this new packed archive are still pending. Prior archive 8d0c2791 runtime results are historical. File downloads are not actual calendar-client imports; desktop widths are not physical devices.

Use `examples/sdk/verify-package.mjs` with a new outside-repository directory, then run the copied examples with each target Node version. Use the retained archive and installed file hashes to establish identity. The static import checker does not establish runtime behavior or independent provenance. No publication, deployment, calendar write or competitive claim occurred.
