# Count candidate browser and Worker execution

Archive `36928444c25a46792517585163d483b7bcc3f0397014bde17ced492495932711` passes six desktop browser runs: Chrome 153, Playwright Firefox 148 and WebKit 26.4 at 320 and 1280 pixels. The runner verifies all installed package bytes and copied browser consumers before execution. Tab/Shift-Tab navigation is exercised; WebKit uses explicit Option-Tab/Shift-Option-Tab. This does not establish native Safari preference behavior, screen-reader usability or physical-phone support.

Each run downloads seven files: date-only point, all-day range, explicit timed range, bounded weekly interval, finite date list, five-occurrence weekly count and three-occurrence monthly count after selecting the last day of shorter months. The counted file exceeds the three-row preview where applicable. Original input remains intact; editing a selected monthly count invalidates its choice and download. Existing clock/list correction, restart, error recovery and blocked ongoing-export checks also pass. No page errors or unexpected external requests were recorded.

The same installed archive executes in local workerd through Wrangler 4.131.1. Its verification handler passes existing failure codes, metadata recovery, stale answers, range/precision checks and both counted files. Installed Worker TypeScript and task-owned lint pass. The copied Worker source matches the recorded repository source. This is not a deployed Cloudflare Worker or hosting security review.

Locked Python icalendar 7.3.0 and recurring-ical-events 3.8.2 read **42 actual browser downloads and eight Worker files**. Counted files contain the exact expected starts and no ongoing rule; readers inspect ten years to catch extra occurrences. Existing scenarios retain their locked expectations. Reader success validates files, not actual calendar-client import or independent language evaluation.

Reproduce with the same verified installation, copied browser/Worker consumers and loopback servers described in `examples/sdk/README.md`. Run `verify-browser.mjs` with `--webkit-option-tab`, then `uv run --locked examples/sdk/read-browser-files.py OUTPUT`. Fetch the task-local Worker `/verify` result and run `uv run --locked examples/sdk/read-worker-files.py REPORT`. Use new report directories. Browser screenshots remain in the original local scratch directory; this retained folder contains reports and all downloaded files.

The initial `pnpm exec vite` startup command failed because the repo exposes it through `vp`; `pnpm exec vp dev` succeeded with the same scratch config. Both task-local servers subsequently exited after interruption; ports 5184 and 8788 had no listeners. Their shutdown ran the repository's existing `prepare` configuration hook. The main app server was not stopped. No publication, deployment, cloud mutation or calendar write occurred.

Remaining gates include count-slot choices for exclusions/past starts, current-candidate comparison replay, physical devices, authorized calendar imports and independent evaluation. Prior performance reports belong to archive `47a218ba`; runtime checks do not establish competitive superiority. The [Node/package evidence](../count-sdk/README.md) is the preceding milestone for this same archive.
