# Current packed SDK after disconnecting

Archive `b8ffb7e04e46eeaaf1e1dcf6c07875b2f805e2497dee50f03280ec0afd14e051` passes the unchanged fourteen-task offline suite in desktop Chrome, Playwright Firefox and WebKit at 320 and 1280 pixels. All six runs pass; separate readers accept 84 downloaded calendar files. The same installed archive also passes all seven Node 22.12.0 examples.

Each browser loads the static packed example, then switches offline. Its deliberate control fetch fails; no subsequent request occurs during clarification, editing or file generation. Original input, keyboard navigation, stale-choice invalidation, unresolved export guards and file contents are checked by the existing runner. Installed archive equality and copied-example hashes are checked before execution.

This suite covers weekly schedules, finite date lists, date precision, explicit ranges, counts and exclusion policies. It does not include the newer quantity, recipient or clock-first reminder journeys. Their targeted Node, main-app and Worker evidence remains separate. WebKit uses Option-Tab; downloads are paced 1.1 seconds apart. This does not establish ordinary-Tab or rapid-download reliability.

Offline begins after module loading. Cold offline website startup, service-worker caching, disconnected installation, physical devices, retail Safari, actual calendar imports and independent task evaluation remain unverified. A desktop width is not a phone. No SDK or app implementation changed in this refresh.

The release checklist's contradictory latest-candidate labels were reconciled into an archive-specific evidence table. Its previous text is retained as release-checklist-before.md. Expectations and the nine matrix requirements remain unchanged.

Reproduce with the static packed example and `verify-browser.mjs --webkit-option-tab --offline-after-load`, followed by `read-browser-files.py`, as documented in examples/sdk/README.md. Raw reports identify versions and hashes; screenshots show the final state rather than every interaction. Hashes establish integrity, not independent certification. Nothing was pushed, deployed, published or written to a calendar.
