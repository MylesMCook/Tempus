# Release-record reconciliation

The earlier task file, matrix and release checklist accumulated status paragraphs for several different artifacts labelled “current” or “latest.” This reconciliation separates current source, packed SDK `36928444`, the older 22-task comparison replay and performance archive `47a218ba`. It does not establish a new product capability or release certification.

`tasks.before.md`, `product-matrix.before.md` and `release-checklist.before.md` preserve the exact pre-edit text. Their original paths and hashes are in `before.json`; relative links inside those historical copies refer to the original file locations. Existing evidence folders and reports were not rewritten.

The current checklist retains all previously unchecked requirements. Its stale illustrative reference to twenty scripted tasks was replaced with “authored development cases and scripted journeys”; the independent-evaluation requirement remains unchanged. The matrix acceptance section, requirement column and milestone exit conditions are preserved. `verification.json` records the checks, current-document link-path validation and retained source/SDK evidence integrity. Link-path checks do not validate web URLs or heading anchors.

No application code, tests, package bytes, services, calendars, cloud settings or evaluation holdouts changed. This is documentation and provenance work. Independent evaluation, physical-device testing, actual calendar imports, written past-start clarification and current-source package verification remain open.
