# Packed SDK: Node evidence

The repository [verification command](../../../examples/sdk/README.md#reproduce-the-node-package-verification) rebuilt and installed SDK archive `4ec180308a16144daa73f3c154d22868f8f47fcd9c4bcebf81d2be2ec1b452df` without publishing it. Both runs produced the same archive as the earlier shorthand candidate.

| Check                                              | Node 22.12.0 | Node 26.8.1 |
| -------------------------------------------------- | ------------ | ----------- |
| Packed files match the installed package           | 56/56        | 56/56       |
| Parser, calendar, state, monthly and list examples | 5/5          | 5/5         |
| Installed browser/calendar/Worker declarations     | Pass         | Pass        |

[Node 22 report](node-22.json), [Node 26 report](node-26.json) and [directory safeguards](controls.json) record exact source/runner/archive hashes and process results. The build and install used pnpm 11.24.0; consumer examples use each named Node executable. The safeguards reject a repository destination and an existing scratch directory without creating or overwriting those paths. A guard refinement distinguishes a `..name` child from an actual parent-directory path.

These are deterministic examples with authored choices. They do not measure general language accuracy, browser/Worker execution, physical devices, calendar-client imports or competitive performance. The report's archive hash identifies output bytes; it does not independently establish compiler or dependency provenance. The scratch installation and tarball are regenerated by the command rather than included here.

To verify retained report bytes, run from this directory:

```sh
shasum -a 256 -c SHA256SUMS
```

Hashes are not signatures or independent expected answers. Reports were formatted before freezing; all result values are unchanged. This evidence remains local and uncommitted; no package was published.
