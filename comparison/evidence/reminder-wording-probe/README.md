# Exploratory reminder wording

Fourteen author-selected short-English inputs were examined against installed archive 4b96da77, after verifying its 56 installed file hashes. The raw report preserves every input, intended task, context and complete result. These are inspected development examples, outside the scored 31-case corpus and untouched evaluation data.

Two ordinary reminders resolve with their full event text: groceries with Jo, and emailing Jo the report. This probe does not exercise their UI correction, export or human completion, so it records no completed tasks.

Two concrete reminder-field gaps deserve implementation work: Buy apples for Sam tomorrow at noon is unsupported, and Buy 3 apples and 2 pears tomorrow at noon is unsupported. The first fails where the event-label boundary treats for as temporal syntax. The second is outside the bounded single-quantity grammar. Neither rejection completes the requested reminder. The next change should support recipient text through explicit title interpretation, full date output and edit invalidation, while preserving duration and recurrence uses of for.

Conditional, approximate, holiday and lower-bound requests do not produce resolved events in these probes. Five responses say needs-clarification but expose no selectable choice; they require editing. That preserves unresolved meaning but does not demonstrate useful guided recovery. A bare negated correction returns no-expression, and two-action input is unsupported. Do not call these rejection counts safety or usability wins, or turn deadlines into exact timestamps to make them pass.

There was no comparator execution, independent author, browser interaction, file validation or calendar-client import in this probe. The existing comparison expectations and product matrix are unchanged. This is a failure-discovery record and an input to the local queue, not a new accuracy ranking. SDK code did not change. The exact ad hoc script is retained compressed; it contains the local installation path and must be adapted to another verified installation. Its raw outputs are not a portable test harness.
