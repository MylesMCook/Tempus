# Explicit-range SDK runtime verification

Archive `47a218ba893908d49fdd132122040df95d107df8508be22fd6ff25a7fad342f4`, September 13, 2026. All 56 installed files match. This adds direct browser and local Worker execution to the [Node/TypeScript/import evidence](../explicit-range-sdk/README.md); package bytes did not change.

Chrome 153.0.8010.36, Firefox 148.0.2 and WebKit 26.4 pass at 320/1280 px. Each run adds explicit numeric-date → repeated-clock correction → complete interval download → edit invalidation to the existing omitted-month, weekly recurrence, date-only and all-day alternative journeys. All 30 actual downloads pass independent readback. The corrected interval starts November 1, 2026 at 07:30 UTC and ends November 2 at 18:00 UTC with the retained title. No browser errors or unexpected external requests were recorded.

Local Wrangler 4.131.1/workerd passes the installed diagnostic Worker and its TypeScript check. The diagnostic retains seven failure codes, metadata/stale recovery, weekly/monthly/list and precision checks, and adds explicit-range correction and stale-edit rejection. All six file strings pass independent readback. Source hashes identify the exact runners and readers. The two changed JavaScript/TypeScript files pass formatting/lint/types without warnings.

WebKit uses explicit Option-Tab. These are desktop viewport and local workerd checks, not physical-device, actual Safari.app, ordinary-Tab, deployed Worker or calendar-client import evidence. Date-only/mixed-precision ranges, occurrence-count limits and ongoing export policies remain open. The separate duration-rule conformance failure is unchanged. Passing these authored checks establishes neither independent accuracy nor competitive superiority.

Reproduce with the repository packed-browser runner and diagnostic Worker against a verified installation, then use their locked Python readers. The browser runner requires the copied example at loopback port 5184 and a new output directory. Preserve prior evidence. No publication, deployment or calendar write occurred.
