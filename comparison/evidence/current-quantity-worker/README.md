# Current packed Worker verification

Archive 4b96da77b994605be06f0c63091c338cb7dae5f5a65e958f3d970786a5858a63 now passes the local Worker diagnostic. All 56 installed files were checked against the package-verification hashes after execution. The copied diagnostic resolves the installed package outside the repository, not the workspace SDK. Its TypeScript check and scoped lint pass.

Diagnostic version 2 adds the quantity reminder through explicit title, numeric-date and repeated-clock answers. It checks retained input/title/span and the complete thirty-minute interval, rejects export at each unresolved stage, and rejects prior choices after changing three apples to four. This is authored control flow, not human interaction evidence.

All seven existing failure categories, metadata recovery, date/list/monthly/count/range checks remain exercised. The separate Python reader matches all sixteen files, including Buy 3 apples on November 1, 2026 from 07:30 to 08:00 UTC. Reader versions are icalendar 7.3.0 and recurring-ical-events 3.8.2. The updated reader also passes the prior version-1 fifteen-file report; older reports do not gain the quantity check retroactively.

Wrangler 4.131.1 ran local workerd 1.20260911.1 on loopback port 8788. The diagnostic returned HTTP 200 and passed. The task-local process was stopped after verification; the main development server was left running. No deployment, external calendar write or account mutation occurred.

This closes the current candidate's scoped local Worker gap. It does not refresh its full offline browser suite, verify a deployed Cloudflare environment, measure energy or prove calendar-client import. Known ongoing-duration conformance, physical-device and independent evaluation gaps remain open. Reproduce with the packed Worker commands in examples/sdk/README.md; diagnostic version 2 returns sixteen files. SDK runtime bytes are unchanged by this verification-tool update.
