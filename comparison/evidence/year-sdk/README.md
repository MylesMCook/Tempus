# Packed shared-year reminder journey

Current archive: `906f3a6f9e0d4cf73cd18e281f0dc9ed2d2b6312b35d3475562a6d39dd8d8004`.

Five installed examples pass on Node 22.12.0 and 26.8.1. The date-list example now completes explicit year selection, shared-time selection, complete two-event file preparation, year replacement and text/timezone/reference invalidation. All 56 installed files match the archive; installed TypeScript consumers, including the updated Worker, compile strictly.

Six browser/viewport runs complete the weekly recovery path and the new yearless reminder path at 320/1280 px. Chrome/Firefox use Tab/Shift-Tab; WebKit explicitly uses Option-Tab. All twelve actual downloads independently read back with exact dates, endpoints, titles and point precision. The default ordinary-Tab WebKit limitation remains separately recorded.

Local workerd through Wrangler 4.131.1 completes the same year/time decision chain, retains source spans, rejects stale answers and produces the two-point file. Its retained seven-failure-code, weekly clock and monthly checks also pass. All three Worker file strings pass independent readback. Worker output is not a browser download or calendar-client import.

The package runner built a new installation. Node 22 directly executed the same five installed examples against that verified installation; it did not repeat the build. Reports record both versions and the archive identity. The browser and Worker consumers resolve the installed package rather than source aliases. Source hashes and exact files are retained here.

[Reproduction commands](../../../examples/sdk/README.md). Use a new packed installation, copy the current browser/Worker example files, and run the explicit WebKit mode for the recorded six-run scope. Both task-local servers were stopped after checking.

This closes the scoped packed integration check for same-month year choice. It does not close mixed-month shorthand, ongoing recurrence policy, physical devices, actual Safari, screen readers, real imports or independent language evaluation. No comparative superiority is claimed.

Run `shasum -a 256 -c SHA256SUMS` here to check retained bytes. Reports were formatted before freezing. The package remains private and local.
